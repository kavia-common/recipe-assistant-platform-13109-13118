# Frontend environment variables (Create React App convention)
# IMPORTANT: Do not put secrets (like Perplexity API keys) in the frontend .env files.
# Frontend variables are exposed to the browser.

# API base for your own backend
REACT_APP_API_BASE_URL=https://your-backend.example.com/api

# Optional WebSocket base if you still use websockets elsewhere
# REACT_APP_WS_BASE_URL=wss://your-backend.example.com

# Secure proxy endpoint for Perplexity requests (handled by your backend)
# The backend should read PERPLEXITY_API_KEY from server-side env and forward requests securely.
REACT_APP_PERPLEXITY_PROXY_URL=/api/ai/perplexity
